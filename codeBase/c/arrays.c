// Including the header file 
#include <stdio.h> 

// Running the main function 
int main() {
    int a, b, c, max; 

    int x[100]; 

    // Assigning 
    x[0] = 10; 
    x[99] = 2; 

    // Using for-loop 
    for (i=0; i< 100; i++)

    // Closing the function 
    return 0; 
}